<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iris.system.pim.dao.SysOrgDao">

	<select id="getList" resultType="com.iris.system.pim.entity.SysOrgEntity">
		select t1.*, (select t2.name from sys_org t2 where t2.id = t1.pid) parentName from sys_org t1 where t1.db_status = 1
			order by t1.sort asc
	</select>

	<select id="getIdAndPidList" resultType="com.iris.system.pim.entity.SysOrgEntity">
		select t1.id, t1.pid from sys_org t1 where t1.db_status = 1
	</select>

</mapper>